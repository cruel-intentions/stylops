@require("./vars.styl")

@page
  name: $name
  author: $author
  main: "index.js"
  version: $version
  license: $license
  description: $description

dependencies
  json2yaml: "^1.1.0"
  stylus: "^0.54.8"

scripts
  test: "echo 'Error: no test specified' && exit 1"
  fixture: "./node_modules/.bin/stylus fixture.styl --print|../../bin/run|node_modules/.bin/json2yaml"
